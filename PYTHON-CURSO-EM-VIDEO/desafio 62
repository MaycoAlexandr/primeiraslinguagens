primeiro = int(input('''Vamos ver a progreção dos 10 primeiros termos de uma PA.
 Diga qual é o primeiro termo: '''))
razao = int(input('Diga qual é a diferença entre um termo e o proxímo: '))
c = 1
termo = primeiro
total=0
mais = 10
while mais != 0:
    total += mais
    while c <= total:
        termo += razao
        c += 1
        print(f'{termo}->',  end=' ')
    print('Pausa')
    mais = int(input('Quantos valores você ainda quer mostrar? '))
print(f'Você viu {total} valores dessa PA.')