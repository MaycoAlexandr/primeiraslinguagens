'''As tuplas são variaveis com mais de um fator, tipo as listas
AS TUPLAS SÃO IMUTAVEIS'''
# o comando index pode falar a posição de um fator na tupla =
# c = ('1', '2', '8')
# print(c.index(8))
# 2

#enumerate = numera fatores, para usala coloque duas variaveis no for
#sorted(x)= ordena os fatores em ordem alfabetica
lanche = ('Hambúrguer', 'suco', 'pizza', 'pudim')
#print(sorted(lanche))

#DUAS FORMAS DE SE FAZER
#for pos, comida in enumerate(lanche):
    #print(f'Eu vou comer {comida}, na posição {pos}')


for cont in range(0, len(lanche)):
    print(lanche[cont])